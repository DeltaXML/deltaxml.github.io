<!DOCTYPE html>
<html lang="en">
	<div class="main">

		<div class="two-thirds column">
			<p>
				<strong>Quick Run</strong> is the fastest way to run an XSLT stylesheet, without first creating a task
				by hand. It can run the stylesheet with any of the three Saxon processors: <strong>SaxonJ</strong>
				(Java), <strong>SaxonJS</strong> (JavaScript) or <strong>SaxonC</strong> (native C/C++). It reuses the
				same command-line arguments the corresponding XSLT task would use, but takes the stylesheet and XML
				source directly from the editor rather than requiring a pre-configured <code>tasks.json</code> entry.
			</p>
			<h3>Running a stylesheet</h3>
			<p>
				With an XSLT file open in the active editor, invoke Quick Run in one of three ways:
			</p>
			<ul>
				<li>Press <span class="shortcut">⌘⌥R</span> (<span class="shortcut">Ctrl+Alt+R</span> on Windows/Linux)</li>
				<li>Click the play button in the editor title bar - its tooltip names the processor currently in use,
					for example <strong>Quick Run with SaxonC</strong></li>
				<li>Run <code>Quick Run with Saxon</code> from the Command Palette</li>
			</ul>
			<p>
				The stylesheet is the active editor's file. The XML source is the current
				<a href="editing-xslt.html#intellisense">XML context file</a> - the same file used to provide XML
				node-names when auto-completing XPath location steps, shown in the status bar. This means the context
				file now serves two purposes: XPath auto-completion, and as the Quick Run source document.
			</p>
			<p>
				An XML context file must be set before Quick Run can be used. Each processor also needs its own setup:
			</p>
			<ul>
				<li><strong>SaxonJ</strong> - the <strong>XSLT.tasks.saxonJar</strong> setting must point at your Saxon
					jar file. See the <strong>Saxon (Java) setup</strong> section on the <a href="run-xslt-prerelease.html#saxonj-tasks">XSLT
					Tasks</a> page.</li>
				<li><strong>SaxonJS</strong> - requires a <a href="https://nodejs.org/en/">NodeJS</a> installation; the
					<code>xslt3</code> package is run using NPX. See the <strong>Saxon-JS Tasks</strong> section on the
					<a href="run-xslt-prerelease.html#saxonjs-tasks">XSLT Tasks</a> page.</li>
				<li><strong>SaxonC</strong> - the <strong>XSLT.tasks.saxonCPath</strong> setting must point at the folder
					containing the SaxonC <code>Transform</code> executable (<code>Transform.exe</code> on Windows). See the
					<strong>SaxonC Tasks</strong> section on the <a href="run-xslt-prerelease.html#saxonc-tasks">XSLT Tasks</a> page for
					setup details, including native library paths and licensing.</li>
			</ul>
			<aside>Tasks for each processor can be turned off with the <strong>XSLT.tasks.java.enabled</strong>,
				<strong>XSLT.tasks.js.enabled</strong> and <strong>XSLT.tasks.saxonc.enabled</strong> settings. Quick Run
				reports an error if the selected processor's tasks are disabled.
			</aside>
			<h3>Choosing the processor: SaxonJ, SaxonJS or SaxonC</h3>
			<p>
				Quick Run uses SaxonJ by default. When an XSLT file is active, the processor currently selected is shown
				in the status bar, for example <strong>▶ SaxonJ</strong>, next to the XML context file. To switch
				processor, use any of the following:
			</p>
			<ul>
				<li>Click the processor name in the status bar</li>
				<li>Hold <span class="shortcut">⌥</span> (<span class="shortcut">Alt</span> on Windows/Linux) and click
					the play button in the editor title bar - while the key is held, the play button changes to a
					'switch' button</li>
				<li>Run <code>XSLT: Quick Run: Switch Processor</code> from the Command Palette</li>
			</ul>
			<p>
				Each of these shows a list of the three processors to pick from, with the current one marked. The play
				button's tooltip and the status bar update straight away.
			</p>
			<p>
				The choice is stored in the <strong>XSLT.tasks.quickRunProcessor</strong> setting, whose values are the
				task types: <code>xslt</code> (SaxonJ), <code>xslt-js</code> (SaxonJS) and <code>xslt-c</code> (SaxonC).
				You can also edit this setting directly, either as a User setting or as a Workspace setting - for
				example, to always use SaxonC for one project. Switching processor updates the setting where it is
				already defined (the workspace folder, then the workspace), otherwise it updates your User settings.
			</p>
			<h3>First use: a task is created for you</h3>
			<p>
				The first time Quick Run is used for a given stylesheet/context-file pairing, a real task is added to
				your workspace's <code>tasks.json</code> file (creating the file if it doesn't exist yet) - not just an
				ephemeral, one-off run. The generated task:
			</p>
			<ul>
				<li>is a normal task for the selected processor: an <code>xslt</code> task for SaxonJ, an
					<code>xslt-js</code> task for SaxonJS or an <code>xslt-c</code> task for SaxonC</li>
				<li>is labelled from the stylesheet and context file names, for example <strong>demo with books.xml</strong>.
					SaxonJS and SaxonC task labels have the processor name added, for example
					<strong>demo with books.xml (SaxonC)</strong></li>
				<li>uses the <code>xslt-xpath.pickResultFile</code> command for its result path, so you're prompted to
					choose (or reuse a recently used) output location - the same 'recent files' behaviour described in the
					<a href="run-xslt-prerelease.html#references">Variable References in Tasks</a> section of the XSLT Tasks page</li>
				<li>refers to the processor's setting (for example <code>${config:XSLT.tasks.saxonCPath}</code>) rather
					than copying its current value, so the task keeps working if you later change the setting</li>
				<li>includes an entry in its <code>parameters</code> array for every top-level <code>xsl:param</code>
					declared with a <code>select</code> default - including params declared in imported or included
					stylesheets - ready for you to edit</li>
			</ul>
			<aside>Parameter values generated this way are prefixed with <code>?</code>, for example <code>?title</code>.
				This tells Saxon to evaluate the value as an XPath expression rather than a literal string, so the
				generated entry reproduces the parameter's own default exactly - it does not silently override it with
				an empty value.
			</aside>
			<h3>Subsequent runs</h3>
			<p>
				Once a task exists for a stylesheet/context-file pairing, Quick Run re-runs that same task rather than
				creating another one. This means you can freely edit the generated task afterwards - change a parameter
				value, adjust the result path, add Saxon features or additional parameters - and Quick Run will keep
				using your edits on every subsequent run, just like running any other task.
			</p>
			<p>
				Tasks are kept separately for each processor. If you switch processor, the next Quick Run for the same
				stylesheet/context-file pairing creates a new task for that processor; your existing tasks for the other
				processors are left unchanged, and are used again if you switch back.
			</p>
			<p>
				If you want a fresh task instead, either edit the existing one directly, or remove it from
				<code>tasks.json</code> and Quick Run will create a new one next time it's used for that
				stylesheet/context-file pairing.
			</p>
			<h3>The XML context file is remembered</h3>
			<p>
				Your chosen XML context file is now restored automatically when Visual Studio Code is reopened, rather
				than defaulting back to the last XML file you happened to view. It's stored per workspace, separately
				from the 'recently used' file list, so viewing other XML files - for example a transform's result - does
				not change what gets restored.
			</p>
			<aside>For custom parameters and features beyond what's auto-populated, chaining tasks together, or any
				other task configuration in detail, see the <a href="run-xslt-prerelease.html">XSLT Tasks</a> page - a task Quick
				Run creates is a normal task, so everything described there still applies to it.
			</aside>
		</div>

	</div>
</html>
